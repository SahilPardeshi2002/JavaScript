"use strict"    // Treat all JS code as new version

// alert(4+6);     // gives error as we are using node js not browser javascript

let age = 22;
let name = "Sahil"
let isLoggedIn = true;

// Datatypes
/*  Primitive datatypes
    1. number  => 2^53
    2. String
    3. boolean  => true or false  
    4. BigInt
    5. null     => Standalone value (representation of empty value)
    6. undefined => variable declared but not assigned any value
    7. symbol    => unique

*/
/*  Non Primitive datatypes
    1.object
    2.arrays
*/

console.log(typeof(undefined));     // gives output as undefined (Itself a datatype)
console.log(typeof null);           // gives output as object   